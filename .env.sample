# AFK GitHub Workflow Configuration
# Copy this file to .env and fill in your values

# ==============================================================================
# REQUIRED: Anthropic Configuration
# ==============================================================================
# Get your API key from: https://console.anthropic.com/settings/keys
# Steps:
#   1. Go to https://console.anthropic.com/settings/keys
#   2. Click "Create Key" (if you don't have one)
#   3. Copy the key (starts with sk-ant-)
#   4. Replace the placeholder below with your actual key
ANTHROPIC_API_KEY=sk-ant-your-key-here

# ==============================================================================
# AUTO-CONFIGURED: These are set automatically during setup
# ==============================================================================
# Port for webhook server (auto-detected by detect_next_port command)
PORT=8001

# GitHub repository URL (auto-configured by setup_github_repo command)
GITHUB_REPO_URL=https://github.com/username/repo

# ==============================================================================
# OPTIONAL: GitHub Configuration
# ==============================================================================
# Personal Access Token - only needed if you want ADW to use a different
# GitHub account than your default 'gh auth login' session.
# If not set, ADW will use the account authenticated with 'gh auth'.
# GITHUB_PAT=ghp_your_personal_access_token_here

# ==============================================================================
# OPTIONAL: Claude Code Configuration
# ==============================================================================
# Path to Claude Code CLI - defaults to 'claude' in PATH
# Only set this if 'claude' command doesn't work, then run 'which claude'
# and paste that value here
CLAUDE_CODE_PATH=claude

# ==============================================================================
# OPTIONAL: Working Directory Behavior
# ==============================================================================
# Returns claude code to the root directory after every command
# Helps maintain consistent working directory during ADW operations
CLAUDE_BASH_MAINTAIN_PROJECT_WORKING_DIR=true
